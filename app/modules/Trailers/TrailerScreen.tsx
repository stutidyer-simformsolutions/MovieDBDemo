import React, { FC } from 'react';
import { Text, View } from 'react-native';

const TrailerScreen: FC = () => {
  return (
    <View>
      <Text>TrailerScreen</Text>
    </View>
  );
};

export default TrailerScreen;
